package render_module.utils

import java.awt.Color

/*
object StyleUtils {
    fun parseColor(colorValue: String): Color {
        return when (colorValue) {
            "red" -> Color.Red
            "blue" -> Color.Blue
            "green" -> Color.Green
            else -> Color.Black
        }
    }

    fun parseFontSize(fontSizeValue: String): TextUnit {
        return fontSizeValue.removeSuffix("px").toInt().sp
    }
}*/
